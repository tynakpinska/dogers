import React from "react";
import "DogOwner.scss";

const DogOwner = ({ open }) => {
  return (
    <div>
        <button>Publish an offer</button>
        <button>Browse dog walkers</button>
    </div>
  );
};

export default DogOwner;
